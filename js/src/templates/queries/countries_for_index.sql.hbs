SELECT
 c.name,
 i.iso,
 i.short_name,
 i.score,
 i.year 
FROM indicator_data i 
  JOIN world_borders c 
    ON i.iso=c.iso3 
WHERE i.short_name = '{{id}}' AND i.year = '{{year}}' 
ORDER BY score desc