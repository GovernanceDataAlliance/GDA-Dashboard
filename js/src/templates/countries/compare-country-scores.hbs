<ul class="{{iso}}">
  {{#each scores}}
    {{#if score}}
      <li class="table-cell {{this.short_name}}">{{this.score}}</li>
    {{else}}
      <li class="table-cell no-data {{this.short_name}}">no data</li>
    {{/if}}
  {{/each}}
</ul>